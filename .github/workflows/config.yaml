on: push
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Load secret
        id: op-load-secret
        uses: 1password/load-secrets-action@v2
        with:
          export-env: false
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
          USERNAME: "op://mfsqhaf3zntu2mgjzaqpdedkba/2iivkygxm7ththmd5k4xcaxhke/username"
          PASSWORD: "op://mfsqhaf3zntu2mgjzaqpdedkba/2iivkygxm7ththmd5k4xcaxhke/password"

      - name: Print masked secret
        run: 'echo "USERNAME: ${{ steps.op-load-secret.outputs.USERNAME }}"
              echo "PASSWORD: ${{ steps.op-load-secret.outputs.PASSWORD }}"
              echo "push'
        # Prints: Secret: ***


