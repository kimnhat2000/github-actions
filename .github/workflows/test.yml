name: Test 1Password CLI Installation

on: push

jobs:
  install-1pw-cli:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # - name: Install 1Password CLI
      #   run: |
      #     curl -sS https://downloads.1password.com/linux/1password-cli.sh | sh
      #     echo "$HOME/.1password" >> $GITHUB_PATH
      #     op --version

      - name: Run test.js
        run: node test.js
